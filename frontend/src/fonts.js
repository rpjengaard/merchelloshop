export default function () {
	// FONTS.COM
	// if (typeof window !== 'undefined') {
	// 	const WebFont = require('webfontloader');

	// 	WebFont.load({
	// 		monotype: {
	// 			projectId: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
	// 			loadAllFonts: true,
	// 		},
	// 	});
	// }

	// TYPEKIT
	// if (typeof window !== 'undefined') {
	// 	const WebFont = require('webfontloader');

	// 	WebFont.load({
	// 		typekit: {
	// 			id: 'xxxxxx',
	// 		},
	// 	});
	// }

	// GOOGLE - just host locally using
	// https://google-webfonts-helper.herokuapp.com/fonts
}

/**
 *
 * Alternatively use webfontloader synchronously inline in index.template.html
 * This avoids a flash of unstyled text
 *
 * <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js"></script>
 * <script>
 *   WebFont.load({
 *     monotype: {
 *       projectId: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
 *       loadAllFonts: true,
 *     },
 *   });
 * </script>
 */
